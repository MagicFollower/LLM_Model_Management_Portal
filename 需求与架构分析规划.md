核心需求：web端的模型管理系统，支持通过API接入模型（优先局域网内本地模型的API）、支持查询并管理模型、支持用户管理与模型权限分配、支持选择模型进行对话、支持知识库与embedding模型、支持知识库上传文档后进行分词拆分内容卡片+拆分流程、支持在对话中显示引用的知识库文档详细信息。

---

附加要求：（100人以内、支持引入完全可本地部署的其他组件），下面是一套结合当前主流生产实践的完整技术架构方案。

---

## 一、架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                      前端 (Vue3 Options API + Vite)               │
│  ┌──────────┬──────────┬──────────┬──────────┬───────────────┐  │
│  │ 模型管理  │ 用户管理  │  对话    │ 知识库    │  权限分配      │  │
│  └──────────┴──────────┴──────────┴──────────┴───────────────┘  │
│         Pinia + Vue Router (RBAC 路由守卫) + Axios               │
└──────────────────────────┬──────────────────────────────────────┘
                           │ REST API (JWT) + SSE 流式
┌──────────────────────────▼──────────────────────────────────────┐
│                   后端 (Fastify + Node.js 20 LTS)                 │
│  ┌──────────┬──────────┬──────────┬──────────────┬────────────┐ │
│  │ 认证授权  │ 模型网关  │ 对话服务  │ 知识库服务    │ 文档处理    │ │
│  │ (JWT)    │ (代理)   │ (SSE)    │ (RAG检索)    │ (异步队列)  │ │
│  └──────────┴──────────┴──────────┴──────────────┴────────────┘ │
│         Prisma ORM  │  BullMQ (异步任务队列)                       │
└───────┬──────────────────┬──────────────────┬───────────────────┘
        │                  │                  │
┌───────▼───────┐  ┌───────▼───────┐  ┌───────▼───────────────────┐
│   MySQL 8.0   │  │    Redis 7    │  │   Xinference + vLLM       │
│  (业务数据)    │  │ (队列+缓存)   │  │  (模型推理 + Embedding     │
│  用户/模型/    │  │ BullMQ 任务   │  │   + Rerank 统一纳管)       │
│  对话/文档元数据│  │ 会话缓存      │  │   统一 OpenAI 兼容 API     │
└───────────────┘  └───────────────┘  └───────────────────────────┘
                                              │
                                    ┌─────────▼─────────┐
                                    │  Qdrant (向量库)   │
                                    │  单进程起步，可按需  │
                                    │  扩展为分片集群     │
                                    └───────────────────┘
```

---

## 二、技术栈最终选型

### 2.1 向量数据库：Qdrant（唯一最优选择）

在 100 人以内、知识库文档量预计在 **百万级 chunk 以内** 的场景下，向量数据库的选择存在明确的最优解：

| 候选 | 定位 | 是否适合本场景 |
|---|---|---|
| pgvector | Postgres 扩展，零新组件 | ❌ 单机扩展上限明显，HNSW 索引构建占用内存，写入与建索引互相争资源 |
| **Qdrant** | **Rust 实现的独立向量库，单二进制起步** | **✅ 最优：部署简单，过滤+混合检索体验完整，单机到小集群扩展平滑** |
| Milvus | 面向十亿级的分布式系统 | ❌ 组件多、运维重，百万级场景属于过度设计 |

Qdrant 的核心优势在于：**部署简单（一个进程即可起步）、过滤与混合检索体验完整、单机到小集群的扩展平滑，运维负担显著低于重型分布式系统**。其基准测试中位延迟仅 **4.55ms**，在功能完备的全栈数据库中延迟最低。

对于本场景，Qdrant 单进程部署即可轻松支撑百万级向量，未来若文档量增长到千万级，直接开启分片模式即可平滑扩展，无需更换技术栈。

### 2.2 模型推理服务：Xinference + vLLM（企业生产环境首选组合）

**这是目前最稳健的企业级架构**：

- **Xinference 作为统一管理平台**：负责对外提供统一的 OpenAI 兼容接口，底层可灵活调用 vLLM、SGLang 等多种推理引擎
- **vLLM 作为推理引擎**：提供高吞吐、PagedAttention 显存管理，适合企业级生产环境

这个组合的核心价值在于 **解耦合**：业务代码只对接 Xinference，底层引擎升级或更换对业务代码无感。同时，**RAG 应用所需的 Embedding 和 Rerank 模型也可以统一纳管，无需额外部署服务**。

**Xinference 全栈模型纳管能力**：

| 模型类型 | 推荐本地模型 | 说明 |
|---|---|---|
| Chat LLM | Qwen2.5-7B-Instruct / DeepSeek-R1-Distill | 通过 vLLM 引擎运行，高吞吐 |
| Embedding | BAAI/bge-small-zh-v1.5 或 bge-large-zh-v1.5 | 中文效果好的本地小模型 |
| Rerank | BAAI/bge-reranker-v2-m3 | 对检索结果二次排序，显著提升 RAG 质量 |

### 2.3 RAG 编排：LangChain.js（Node.js 原生）

虽然 Python 生态的 LangChain 更成熟，但考虑到你的后端是 Node.js 同栈，推荐使用 **LangChain.js**，它提供了与 Python 版对等的 RAG 能力（文档加载器、文本分割器、检索链、重排序集成），且与 Fastify 天然集成。

**RAG 技术栈 2026 主流组合**：
- 编排框架：LangChain 0.3.x
- 向量数据库：Qdrant（生产）
- Embedding：BAAI/bge-small-zh-v1.5（中文优化）
- LLM：Qwen2.5 或 DeepSeek（OpenAI 兼容接口）

### 2.4 异步任务队列：BullMQ + Redis

文档处理流水线（解析 → 切分 → 向量化 → 入库）是典型的异步任务。BullMQ 基于 Redis，提供：

- 任务重试、进度追踪、失败回滚
- 与 Fastify 通过 `@fastify/bullmq` 无缝集成
- 支持并发控制，避免大量文档同时上传时压垮 embedding 模型

### 2.5 SSE 流式输出：@fastify/sse

Fastify 官方 SSE 插件提供一等支持：

- 路由级 SSE 支持，只需设置 `{ sse: 'manual' }`
- 原生支持 Node.js streams 和 async iterators
- 内置心跳保活、断线重连（Last-Event-ID）
- 完整 TypeScript 类型定义

### 2.6 前端 UI 与图标

- **UI 组件库**：PrimeVue + Poseidon 主题（扁平化设计，Light 模式原生支持）
- **图标库**：**Reicon Vue**（2676+ 像素级对齐 SVG 图标，Outline & Filled 双权重，零依赖，tree-shakeable，MIT 协议）
- **RBAC 权限**：vue-accessguard 或 vue-rbac（轻量级，支持指令式权限控制、角色继承、缓存）

---

## 三、知识库与 RAG 流水线（核心设计）

### 3.1 文档处理六阶段流水线

```
上传 → 解析 → 智能切分 → 向量化 → 入库 → 检索重排
```

**阶段 1：文档解析**
- 支持 PDF、Word、Markdown、TXT
- 使用 LangChain.js 的 DocumentLoader，按文件类型自动选择解析器
- 保留页码、标题层级等元数据

**阶段 2：智能切分**

采用 `RecursiveCharacterTextSplitter` 策略：
- chunk_size = 500，chunk_overlap = 50（中文文档推荐值）
- 切分边界优先级：`\n\n` → `\n` → `。` → `；` → 空格
- 每个 chunk 保留元数据：`{ doc_id, page, section_title, chunk_index }`

**阶段 3：向量化**

调用 Xinference 纳管的 embedding 模型，批量生成向量。注意维度必须与 Qdrant collection 的维度严格一致。

**阶段 4：入库 Qdrant**

```javascript
// Qdrant 支持 payload 过滤，检索时可先按 kb_id 过滤再向量检索
await qdrant.upsert('knowledge_chunks', {
  points: chunks.map(c => ({
    id: c.id,
    vector: c.embedding,
    payload: {
      doc_id: c.doc_id,
      kb_id: c.kb_id,
      content: c.content,
      page: c.page,
      section_title: c.section_title
    }
  }))
})
```

**阶段 5：混合检索**

Qdrant 内置稀疏向量与 BM25 混合检索能力，结合向量相似度与关键词匹配，召回质量显著优于纯向量检索。

**阶段 6：重排序**

使用 Xinference 纳管的 bge-reranker-v2-m3 对 Top-K 结果进行二次排序，将最相关的 chunk 排到最前面。

### 3.2 对话中的引用展示

检索到的 chunk 随 LLM 响应一起返回：

```javascript
const citations = chunks.map(c => ({
  doc_name: c.payload.file_name,
  page: c.payload.page,
  section_title: c.payload.section_title,
  content: c.payload.content.substring(0, 300),
  score: c.score
}))
```

前端在助手消息下方展示可折叠的引用卡片，显示文档名、页码、章节标题、相似度分数和内容片段。

---

## 四、部署架构（Docker Compose）

```yaml
version: '3.8'

services:
  # 业务数据库
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: model_manager
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"

  # 队列 + 缓存
  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"

  # 向量数据库
  qdrant:
    image: qdrant/qdrant:latest
    volumes:
      - qdrant_data:/qdrant/storage
    ports:
      - "6333:6333"

  # 模型推理平台（纳管 vLLM + Embedding + Rerank）
  xinference:
    image: xprobe/xinference:latest
    volumes:
      - model_cache:/root/.cache
    ports:
      - "9997:9997"
    command: xinference-local --host 0.0.0.0 --port 9997

  # 后端
  backend:
    build: ./backend
    environment:
      DATABASE_URL: mysql://root:${MYSQL_ROOT_PASSWORD}@mysql:3306/model_manager
      REDIS_URL: redis://redis:6379
      QDRANT_URL: http://qdrant:6333
      XINFERENCE_URL: http://xinference:9997
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      - mysql
      - redis
      - qdrant
      - xinference
    ports:
      - "3000:3000"

  # 前端
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mysql_data:
  redis_data:
  qdrant_data:
  model_cache:
```

**资源规划建议（100人以内）**：

| 组件 | CPU | 内存 | 磁盘 | GPU |
|---|---|---|---|---|
| MySQL | 2C | 4GB | 50GB | — |
| Redis | 1C | 2GB | 10GB | — |
| Qdrant | 4C | 8GB | 100GB | — |
| Xinference | 8C | 32GB | 500GB | 1×RTX 4090 或 A10 |
| Backend | 4C | 8GB | — | — |
| Frontend | 1C | 1GB | — | — |

---

## 五、关键设计决策与理由

**决策 1：为什么不用 MySQL VECTOR 而用 Qdrant？**
MySQL 8.0.31+ 的 VECTOR 类型适合万级以下向量，但百万级 chunk 后 HNSW 索引构建会占用大量内存，写入与建索引互相争资源，且无内置混合检索能力。Qdrant 单进程即可支撑百万级向量，且原生支持 payload 过滤和混合检索，运维成本仅增加一个容器。

**决策 2：为什么用 Xinference 而不是直接接 Ollama？**
Ollama 并发能力较弱，不适合企业级高并发生产环境。Xinference 可以同时纳管 Chat LLM、Embedding、Rerank 三类模型，提供统一的 OpenAI 兼容接口，且底层可切换推理引擎。100 人规模下，统一的模型管理平台是必需的。

**决策 3：为什么用 BullMQ 而不是在请求中同步处理？**
文档处理（尤其是向量化）耗时较长，同步处理会导致 HTTP 请求超时。BullMQ 提供可靠的任务持久化、重试机制和进度追踪，用户可以上传后关闭页面，处理完成后收到通知。

**决策 4：为什么用 Reicon Vue 而不是其他图标库？**
Reicon Vue 提供 2676+ 像素级对齐的 SVG 图标，双权重（Outline/Filled），零依赖，完全 tree-shakeable，MIT 协议，与你的“扁平化白色主题”要求完美契合。

**决策 5：为什么前后端不分离部署到不同域名？**
100 人规模内部系统，前端 Nginx 反向代理 `/api` 到后端即可，无需 CORS 配置，简化部署和运维。SSE 流式接口也天然避免跨域问题。

---

## 六、开发路线图

| 迭代 | 内容 | 工期 |
|---|---|---|
| **迭代 1** | 项目骨架 + 认证 + 用户管理 + RBAC + 模型 CRUD（含 Xinference 接入） | 2 周 |
| **迭代 2** | 对话界面 + SSE 流式 + 会话历史 + 模型选择 | 1.5 周 |
| **迭代 3** | 知识库 CRUD + 文档上传 + BullMQ 异步处理 + 切分可视化 | 2 周 |
| **迭代 4** | Qdrant 集成 + Embedding + Rerank + 引用展示 | 2 周 |
| **迭代 5** | 打磨 + Docker Compose 部署 + 性能调优 | 1.5 周 |

**总计约 9 周**，可交付一套完整可用的内网模型管理系统。